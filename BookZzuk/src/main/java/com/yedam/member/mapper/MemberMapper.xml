<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.member.mapper.MemberMapper">
	<select id="login" resultType="MemberVO">
		select * from member where user_id=#{userId} and user_pw=#{userPw}
	</select>
	
	<insert id="addMember" parameterType="MemberVO">
		insert into member(user_id, user_pw, name, age, addr, email, phone)
		values(#{userId}, #{userPw}, #{name}, #{age}, #{addr}, #{email}, #{phone})
	</insert>
	
	<select id="memberList" resultType="MemberVO">
		select * from member order by user_id
	</select>
	
	<select id="getMember" parameterType="string" resultType="MemberVO">
		select * from member where user_id=#{id}
	</select>
	
	<update id="updateMember" parameterType="MemberVO">
		update member
		<set>
		<if test="memberPw != null">
			user_pw = #{userPw},
		</if>
		<if test="memberName != null">
			name = #{userName},
		</if>
		<if test="memberPhone != null">
			phone = #{userPhone},
		</if>
		<if test="memberAddr != null">
			addr = #{userAddr},
		</if>
		<if test="memberEmail != null">
			email = #{userEmail},
		</if>

		</set> 
		where user_id = #{userId}
	</update>
	
	<delete id="deleteMember" parameterType="string">
		delete from member
		where user_id = #{userId}
	</delete>
	
</mapper>