<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.member.mapper.MemberMapper">
	<select id="login" resultType="MemberVO">
		select * from member where user_id=#{userId} and user_pw=#{userPw}
	</select>
	
	<insert id="addMember" parameterType="MemberVO">
		insert into member(user_id, user_pw, addr, email, phone, name, age, grade)
		values(#{userId},#{userPw},#{userAddr}, #{userEmail}, #{userPhone}, #{userName}, #{userAge}, #{userGrade})
	</insert>
	
	<select id="memberList" resultType="MemberVO">
		select * from member order by user_id
	</select>
	
	<select id="getMember" parameterType="string" resultType="MemberVO">
		select * from member where user_id=#{id}
	</select>
	
	<update id="updateMember" parameterType="MemberVO">
		update member
		<set>
		<if test="userPw != null">
			user_pw = #{userPw},
		</if>
		<if test="name != null">
			name = #{name},
		</if>
		<if test="addr != null">
			addr = #{addr},
		</if>
		<if test="phone != null">
			phone = #{phone},
		</if>
		<if test="email != null">
			email = #{email},
		</if>
		</set> 
		where user_id = #{userId}
	</update>
	
	<delete id="deleteMember" parameterType="string">
		delete from member
		where user_id = #{mid}
	</delete>
	
</mapper>